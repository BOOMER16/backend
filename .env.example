# Copy this file to .env and fill in your key to enable LIVE Gemini calls.
# Leave GEMINI_API_KEY unset (or delete the .env file) to run in safe MOCK
# mode -- recommended for the actual stage demo so venue wifi/API hiccups
# can never break your live run.
GEMINI_API_KEY=
